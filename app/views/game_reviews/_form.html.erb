<%= form_with(model: game_review) do |form| %>
  <% if game_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_review.errors.count, "error") %> prohibited this game_review from being saved:</h2>

      <ul>
        <% game_review.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.number_field :game_id %>
  </div>

  <div class="field">
    <%= form.label :review %>
    <%= form.text_area :review %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
